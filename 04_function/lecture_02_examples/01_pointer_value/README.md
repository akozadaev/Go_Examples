# 01_pointer_value

Самостоятельный проект к лекции 2.

Показывает:

- передачу аргументов по значению;
- изменение состояния через указатель;
- безопасную обработку `nil`;
- тесты на mutation и граничные случаи.

Команды:

```bash
go test ./...
go run ./cmd/demo
```
