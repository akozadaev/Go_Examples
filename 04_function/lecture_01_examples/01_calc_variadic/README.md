# 01_calc_variadic

Самостоятельный проект к лекции 1.

Показывает:

- variadic-параметры;
- возврат `error`;
- функцию как аргумент;
- базовые unit-тесты.

Команды:

```bash
go test ./...
go run ./cmd/demo
```
