# 02_closures

Самостоятельный проект к лекции 2.

Показывает:

- замыкание со счетчиком;
- фабрику функций;
- независимое состояние разных замыканий;
- тесты на последовательные вызовы.

Команды:

```bash
go test ./...
go run ./cmd/demo
```
