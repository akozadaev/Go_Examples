# 03_scope_shadowing

Самостоятельный проект к лекции 2.

Показывает:

- область видимости блочных переменных;
- полезное ограничение scope;
- типичную ошибку shadowing через `:=`;
- исправленный вариант без затенения.

Команды:

```bash
go test ./...
go run ./cmd/demo
```
