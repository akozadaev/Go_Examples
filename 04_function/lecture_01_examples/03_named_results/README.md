# 03_named_results

Самостоятельный проект к лекции 1.

Показывает:

- несколько возвращаемых значений;
- именованные результаты;
- явную ошибку для невалидного ввода;
- аккуратное использование zero value.

Команды:

```bash
go test ./...
go run ./cmd/demo
```
